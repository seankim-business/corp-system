# AR Dashboard UI & Slack Commands 구현 기획서

## 개요

**프로젝트명:** AR (Agent Resource) Management Dashboard & Slack Integration
**버전:** 1.0.0
**작성일:** 2026-01-30
**예상 기간:** 3-5일
**우선순위:** HIGH

### 목표
기존 백엔드 AR API를 활용하여 프론트엔드 대시보드 UI와 Slack 명령어 인터페이스를 구현합니다.

---

## 1. 전체 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              사용자 인터페이스                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────┐    ┌─────────────────────────────┐        │
│  │     Frontend Dashboard      │    │      Slack Integration      │        │
│  │                             │    │                             │        │
│  │  ┌──────────┐ ┌──────────┐ │    │  ┌──────────┐ ┌──────────┐ │        │
│  │  │ AR 대시  │ │ 부서관리 │ │    │  │ /ar 명령 │ │Interactive│ │        │
│  │  │  보드    │ │ 페이지   │ │    │  │   처리   │ │ Buttons  │ │        │
│  │  └──────────┘ └──────────┘ │    │  └──────────┘ └──────────┘ │        │
│  │  ┌──────────┐ ┌──────────┐ │    │  ┌──────────┐ ┌──────────┐ │        │
│  │  │ 포지션   │ │ 배치관리 │ │    │  │ 한글     │ │ 알림     │ │        │
│  │  │ 페이지   │ │ 페이지   │ │    │  │ 명령어   │ │ 서비스   │ │        │
│  │  └──────────┘ └──────────┘ │    │  └──────────┘ └──────────┘ │        │
│  │  ┌──────────┐ ┌──────────┐ │    │                             │        │
│  │  │ 결재관리 │ │ 분석/    │ │    │                             │        │
│  │  │ 페이지   │ │ 리포트   │ │    │                             │        │
│  │  └──────────┘ └──────────┘ │    │                             │        │
│  │  ┌──────────┐              │    │                             │        │
│  │  │ 워크로드 │              │    │                             │        │
│  │  │ 뷰어     │              │    │                             │        │
│  │  └──────────┘              │    │                             │        │
│  └─────────────────────────────┘    └─────────────────────────────┘        │
│                │                                 │                          │
└────────────────│─────────────────────────────────│──────────────────────────┘
                 │                                 │
                 ▼                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API Layer (Express)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                      Existing AR API Routes                        │    │
│  │                                                                    │    │
│  │  /api/ar/departments     /api/ar/positions     /api/ar/assignments │    │
│  │  /api/ar/coordination    /api/ar/analytics                         │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                      New Slack Commands Router                     │    │
│  │                                                                    │    │
│  │  /api/slack/ar-commands   (신규 생성)                              │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Services Layer                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │ arDepartment   │  │ arPosition     │  │ arAssignment   │                │
│  │ Service        │  │ Service        │  │ Service        │                │
│  └────────────────┘  └────────────────┘  └────────────────┘                │
│                                                                             │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │ arApproval     │  │ Coordination   │  │ arSlackNotifier│                │
│  │ Service        │  │ Services       │  │ Service        │                │
│  └────────────────┘  └────────────────┘  └────────────────┘                │
│                                                                             │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │ Meta-Agents    │  │ Recommendation │  │ DailyReport    │                │
│  │ (Analyst/Coach)│  │ Engine         │  │ Service        │                │
│  └────────────────┘  └────────────────┘  └────────────────┘                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Data Layer (Prisma)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ARDepartment  |  ARPosition  |  ARAssignment  |  ARApprovalRequest        │
│  Agent         |  AgentDailyReport  |  GoalAlignmentRecord  |  Task        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 파일 구조 상세

### 2.1 Frontend 신규 파일

```
frontend/src/
├── pages/
│   └── ar/                               # AR 관련 페이지 그룹
│       ├── ARDashboardPage.tsx           # AR 메인 대시보드
│       ├── ARDepartmentsPage.tsx         # 부서 관리
│       ├── ARPositionsPage.tsx           # 포지션 관리
│       ├── ARAssignmentsPage.tsx         # 배치 관리
│       ├── ARApprovalsPage.tsx           # AR 결재 관리
│       ├── ARAnalyticsPage.tsx           # 분석 및 리포트
│       └── ARWorkloadPage.tsx            # 워크로드 뷰어
│
├── components/
│   └── ar/                               # AR 관련 컴포넌트
│       ├── ARStatCard.tsx                # AR 전용 통계 카드
│       ├── DepartmentTree.tsx            # 부서 계층 트리 뷰
│       ├── DepartmentCard.tsx            # 부서 카드 컴포넌트
│       ├── PositionCard.tsx              # 포지션 카드 컴포넌트
│       ├── AssignmentCard.tsx            # 배치 카드 컴포넌트
│       ├── WorkloadChart.tsx             # 워크로드 차트
│       ├── WorkloadDistributionBar.tsx   # 워크로드 분포 바
│       ├── AgentHealthIndicator.tsx      # 에이전트 상태 표시기
│       ├── ApprovalChainView.tsx         # 결재 체인 시각화
│       ├── IssueAlert.tsx                # 이슈 알림 컴포넌트
│       ├── RecommendationCard.tsx        # 추천 카드
│       ├── CreateDepartmentModal.tsx     # 부서 생성 모달
│       ├── CreatePositionModal.tsx       # 포지션 생성 모달
│       ├── CreateAssignmentModal.tsx     # 배치 생성 모달
│       ├── EditDepartmentModal.tsx       # 부서 수정 모달
│       ├── EditPositionModal.tsx         # 포지션 수정 모달
│       └── EditAssignmentModal.tsx       # 배치 수정 모달
│
├── hooks/
│   └── ar/                               # AR 관련 커스텀 훅
│       ├── useARDepartments.ts           # 부서 데이터 훅
│       ├── useARPositions.ts             # 포지션 데이터 훅
│       ├── useARAssignments.ts           # 배치 데이터 훅
│       ├── useARApprovals.ts             # 결재 데이터 훅
│       ├── useARWorkload.ts              # 워크로드 데이터 훅
│       ├── useARAnalytics.ts             # 분석 데이터 훅
│       └── useARIssues.ts                # 이슈 데이터 훅
│
└── types/
    └── ar.ts                             # AR 타입 정의
```

### 2.2 Backend 신규 파일

```
src/
├── api/
│   └── slack-ar-commands.ts              # AR Slack 명령어 핸들러 (신규)
│
└── services/
    └── slack-ar-command.service.ts       # AR 명령어 처리 서비스 (신규)
```

### 2.3 수정 필요 파일

```
frontend/src/
├── App.tsx                               # 라우트 추가
└── components/
    └── layout/
        └── Sidebar.tsx                   # 네비게이션 메뉴 추가

src/
└── index.ts                              # Slack 라우트 등록
```

---

## 3. 각 페이지 컴포넌트 설계

### 3.1 ARDashboardPage.tsx - AR 메인 대시보드

**목적:** AR 시스템의 전체 현황을 한눈에 파악

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ AR Dashboard                                                 │
│ Agent Resource Management Overview                           │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 총 부서  │ │ 총 포지션│ │ 활성 배치│ │ 대기 결재│       │
│  │    5     │ │    12    │ │    8     │ │    3     │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│  ┌────────────────────────────┐ ┌─────────────────────────┐ │
│  │     워크로드 분포          │ │     최근 이슈           │ │
│  │  ████████░░ 78%           │ │  🔴 과부하 에이전트 2   │ │
│  │  ██████░░░░ 62%           │ │  🟡 목표 미달 1         │ │
│  │  ████████████ 95%         │ │  🟢 전체 정상           │ │
│  └────────────────────────────┘ └─────────────────────────┘ │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                   추천 사항                           │   │
│  │  💡 팀A에 추가 에이전트 배치 권장                     │   │
│  │  💡 프로젝트X 마감 리스크 - 우선순위 조정 필요       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**데이터 소스:**
- `GET /api/ar/departments` - 부서 수
- `GET /api/ar/positions` - 포지션 수
- `GET /api/ar/assignments?status=active` - 활성 배치 수
- `GET /api/ar/coordination/issues/summary` - 이슈 요약
- `GET /api/ar/coordination/workload` - 워크로드 분포
- `GET /api/ar/analytics/recommendations` - 추천 사항

**상태 관리:**
```typescript
interface ARDashboardState {
  stats: {
    totalDepartments: number;
    totalPositions: number;
    activeAssignments: number;
    pendingApprovals: number;
  };
  workloadDistribution: WorkloadSnapshot[];
  recentIssues: Issue[];
  recommendations: Recommendation[];
  isLoading: boolean;
  error: string | null;
}
```

---

### 3.2 ARDepartmentsPage.tsx - 부서 관리

**목적:** 부서 CRUD 및 계층 구조 관리

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ Departments                              [+ 부서 추가]       │
│ 조직의 부서 구조를 관리합니다                                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  필터: [상태 ▼] [검색...]                                   │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 📁 Engineering                           [수정] [삭제] │ │
│  │    └── 📁 Backend Team                   [수정] [삭제] │ │
│  │    └── 📁 Frontend Team                  [수정] [삭제] │ │
│  │ 📁 Operations                            [수정] [삭제] │ │
│  │    └── 📁 DevOps                         [수정] [삭제] │ │
│  │ 📁 Marketing                             [수정] [삭제] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 선택된 부서: Engineering                              │   │
│  │                                                       │   │
│  │ 설명: 제품 개발 부서                                  │   │
│  │ 상태: active                                          │   │
│  │ 예산: $50,000                                         │   │
│  │ 하위 부서: 2개                                        │   │
│  │ 포지션: 5개                                           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 연동:**
```typescript
// 부서 목록 조회
GET /api/ar/departments?status=active&search=...

// 부서 생성
POST /api/ar/departments
Body: { name, description, parentId?, status }

// 부서 수정
PATCH /api/ar/departments/:id
Body: { name?, description?, parentId?, status? }

// 부서 삭제
DELETE /api/ar/departments/:id

// 부서 계층 조회
GET /api/ar/departments/:id/hierarchy

// 부서 예산 수정
PATCH /api/ar/departments/:id/budget
Body: { budgetCents }
```

---

### 3.3 ARPositionsPage.tsx - 포지션 관리

**목적:** 포지션 CRUD 및 요구 스킬 관리

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ Positions                                [+ 포지션 추가]     │
│ 에이전트가 맡을 수 있는 역할을 정의합니다                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  필터: [부서 ▼] [상태 ▼] [검색...]                         │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 포지션명        │ 부서      │ 배치됨 │ 상태  │ 액션    ││
│  ├─────────────────────────────────────────────────────────┤│
│  │ Senior Dev      │ Backend   │  2/3   │ active│ [▼]     ││
│  │ Junior Dev      │ Backend   │  1/2   │ active│ [▼]     ││
│  │ DevOps Engineer │ DevOps    │  1/1   │ active│ [▼]     ││
│  │ Data Analyst    │ Analytics │  0/2   │ open  │ [▼]     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [포지션 상세]                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ Senior Developer                                      │   │
│  │                                                       │   │
│  │ 요구 스킬:                                            │   │
│  │  • TypeScript (expert)                               │   │
│  │  • Node.js (advanced)                                │   │
│  │  • AWS (intermediate)                                │   │
│  │                                                       │   │
│  │ 최소 경험: 3년                                        │   │
│  │ 정원: 3명 (현재 2명 배치)                            │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 연동:**
```typescript
// 포지션 목록 조회
GET /api/ar/positions?departmentId=...&status=...

// 포지션 생성
POST /api/ar/positions
Body: {
  title, departmentId, description,
  requiredSkills: [{ name, level, weight }],
  minExperience, maxCapacity, status
}

// 포지션 수정
PATCH /api/ar/positions/:id

// 포지션 삭제
DELETE /api/ar/positions/:id
```

---

### 3.4 ARAssignmentsPage.tsx - 배치 관리

**목적:** 에이전트-포지션 배치 관리

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ Assignments                              [+ 배치 추가]       │
│ 에이전트를 포지션에 배치하고 관리합니다                     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  필터: [상태 ▼] [부서 ▼] [유형 ▼]                          │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 에이전트        │ 포지션     │ 유형     │ 워크로드│상태 ││
│  ├─────────────────────────────────────────────────────────┤│
│  │ agent-alpha     │ Senior Dev │permanent │  85%   │active││
│  │ agent-beta      │ Junior Dev │temporary │  62%   │active││
│  │ agent-gamma     │ DevOps     │permanent │  95%   │active││
│  │ agent-delta     │ Analyst    │acting    │  45%   │leave ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [배치 상세]                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ agent-alpha → Senior Developer                        │   │
│  │                                                       │   │
│  │ 배치 유형: permanent                                  │   │
│  │ 시작일: 2024-01-15                                    │   │
│  │ 담당자: @john.doe                                     │   │
│  │ 성과 점수: 87/100                                     │   │
│  │ 워크로드: 85%                                         │   │
│  │                                                       │   │
│  │ [재배치] [상태변경] [종료]                           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 연동:**
```typescript
// 배치 목록 조회
GET /api/ar/assignments?status=...&departmentId=...

// 배치 생성
POST /api/ar/assignments
Body: {
  agentId, positionId, humanSupervisor?,
  assignmentType, startDate?, endDate?,
  status, workload
}

// 배치 수정
PATCH /api/ar/assignments/:id

// 재배치
POST /api/ar/assignments/:id/reassign
Body: { newPositionId }

// 상태 변경
PATCH /api/ar/assignments/:id/status
Body: { status }

// 배치 종료
POST /api/ar/assignments/:id/terminate
Body: { reason? }
```

---

### 3.5 ARApprovalsPage.tsx - AR 결재 관리

**목적:** AR 관련 결재 요청 관리 (5단계 결재 시스템)

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ AR Approvals                                                 │
│ 에이전트 리소스 관련 결재를 관리합니다                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  필터: [상태 ▼] [유형 ▼] [레벨 ▼]                          │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 제목              │ 유형    │레벨│ 요청자  │ 상태      ││
│  ├─────────────────────────────────────────────────────────┤│
│  │ 예산 증액 요청    │ budget  │ 3 │ agent-a │ 🟡 pending││
│  │ 신규 배치 요청    │ assign  │ 2 │ agent-b │ 🟡 pending││
│  │ 일정 변경 요청    │ schedule│ 1 │ agent-c │ ✅ approved│
│  │ 휴가 신청         │ leave   │ 1 │ agent-d │ ❌ rejected│
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [결재 상세]                                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 예산 증액 요청                                        │   │
│  │                                                       │   │
│  │ 요청자: agent-alpha                                   │   │
│  │ 레벨: 3 (PROJECT)                                     │   │
│  │ 설명: Q2 마케팅 예산 $5,000 증액 필요                │   │
│  │ 영향 범위: team                                       │   │
│  │ 예상 가치: $5,000                                     │   │
│  │                                                       │   │
│  │ 결재 체인:                                            │   │
│  │  1️⃣ direct_supervisor ✅                             │   │
│  │  2️⃣ team_lead ✅                                      │   │
│  │  3️⃣ project_manager 🟡 (현재)                        │   │
│  │                                                       │   │
│  │ [승인] [반려] [에스컬레이션]                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**결재 레벨:**
- Level 1 (TASK): direct_supervisor 또는 자동 승인
- Level 2 (PROCESS): team_lead
- Level 3 (PROJECT): project_manager
- Level 4 (FUNCTION): function_owner
- Level 5 (OBJECTIVE): c_level

---

### 3.6 ARAnalyticsPage.tsx - 분석 및 리포트

**목적:** AR 시스템 성과 분석 및 인사이트 제공

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ AR Analytics                              기간: [30일 ▼]    │
│ 에이전트 리소스 성과 분석                                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [성과 지표]                                                 │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ 작업 완료 │ │ 평균 성과 │ │ 비용 효율 │ │ 목표 달성 │   │
│  │   156     │ │   82%     │ │   $0.45   │ │   74%     │   │
│  │ +12%      │ │ +5%       │ │ -8%       │ │ +3%       │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│                                                              │
│  [에이전트 건강 상태]                                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 에이전트     │ 상태    │ 워크로드 │ 이슈          │   │   │
│  │ agent-alpha  │ 🟢 정상 │ ████░    │ 없음          │   │   │
│  │ agent-beta   │ 🟡 주의 │ ████████ │ 과부하 경고   │   │   │
│  │ agent-gamma  │ 🔴 위험 │ ██████████│ 긴급 조치 필요│   │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [부서별 비용]                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Engineering  ████████████████  $12,450              │   │
│  │  Operations   ████████         $6,230                │   │
│  │  Marketing    ██████           $4,890                │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [코칭 필요 에이전트]                                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • agent-beta: 성과 리뷰 필요                         │   │
│  │ • agent-delta: 스킬 개발 권장                        │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 연동:**
```typescript
// 분석 리포트
GET /api/ar/analytics/report?periodDays=30

// 특정 메트릭
GET /api/ar/analytics/metric/:metric  // workload, performance, cost, completion

// 운영 일일 체크
GET /api/ar/analytics/ops/daily

// 건강 상태 체크
GET /api/ar/analytics/ops/health

// 코칭 필요 평가
GET /api/ar/analytics/coaching/needs
```

---

### 3.7 ARWorkloadPage.tsx - 워크로드 뷰어

**목적:** 워크로드 분포 시각화 및 리밸런싱

**레이아웃:**
```
┌──────────────────────────────────────────────────────────────┐
│ Workload Distribution                    [리밸런싱 제안]    │
│ 에이전트 워크로드를 모니터링하고 최적화합니다               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [워크로드 통계]                                             │
│  평균: 72% | 표준편차: 15% | 과부하: 2 | 저활용: 1         │
│                                                              │
│  [에이전트별 워크로드]                                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ agent-alpha   ████████████████░░░░  82%   [정상]    │   │
│  │ agent-beta    ████████████████████  95%   [과부하]  │   │
│  │ agent-gamma   ████████████░░░░░░░░  58%   [정상]    │   │
│  │ agent-delta   ████████████████████  98%   [위험]    │   │
│  │ agent-epsilon ████░░░░░░░░░░░░░░░░  22%   [저활용]  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  [리밸런싱 제안]                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 🔄 제안된 변경사항:                                  │   │
│  │                                                       │   │
│  │ 1. agent-delta → agent-epsilon 으로 작업 3건 이전    │   │
│  │    예상 효과: delta 98%→72%, epsilon 22%→45%         │   │
│  │                                                       │   │
│  │ 2. agent-beta → agent-gamma 으로 작업 2건 이전       │   │
│  │    예상 효과: beta 95%→78%, gamma 58%→71%            │   │
│  │                                                       │   │
│  │ [제안 적용] [부분 적용] [취소]                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**API 연동:**
```typescript
// 워크로드 분석
GET /api/ar/coordination/workload

// 리밸런싱 제안 생성
POST /api/ar/coordination/workload/rebalance
Body: { trigger?: 'manual' | 'overload' | ... }

// 제안 적용
POST /api/ar/coordination/workload/apply
Body: { proposalId, partial?, changeIds? }
```

---

## 4. API 연동 명세

### 4.1 타입 정의 (frontend/src/types/ar.ts)

```typescript
// =============================================================================
// DEPARTMENT TYPES
// =============================================================================

export type DepartmentStatus = 'active' | 'inactive' | 'archived';

export interface ARDepartment {
  id: string;
  organizationId: string;
  name: string;
  description?: string | null;
  parentId?: string | null;
  headPositionId?: string | null;
  budgetCents: number;
  status: DepartmentStatus;
  metadata?: Record<string, unknown>;
  createdAt: string;
  updatedAt: string;
}

export interface DepartmentHierarchy {
  department: ARDepartment;
  children: ARDepartment[];
  ancestors: ARDepartment[];
}

export interface CreateDepartmentInput {
  name: string;
  description?: string;
  parentId?: string;
  headPositionId?: string;
  status?: DepartmentStatus;
  metadata?: Record<string, unknown>;
}

export interface UpdateDepartmentInput {
  name?: string;
  description?: string | null;
  parentId?: string | null;
  headPositionId?: string | null;
  status?: DepartmentStatus;
  metadata?: Record<string, unknown>;
}

// =============================================================================
// POSITION TYPES
// =============================================================================

export type PositionStatus = 'active' | 'inactive' | 'archived';
export type SkillLevel = 'beginner' | 'intermediate' | 'advanced' | 'expert';

export interface RequiredSkill {
  name: string;
  level: SkillLevel;
  weight: number;
}

export interface ARPosition {
  id: string;
  organizationId: string;
  departmentId: string;
  title: string;
  description?: string | null;
  requiredSkills: RequiredSkill[];
  minExperience: number;
  maxCapacity: number;
  currentCount: number;
  status: PositionStatus;
  reportsTo?: string | null;
  metadata?: Record<string, unknown>;
  createdAt: string;
  updatedAt: string;
}

export interface CreatePositionInput {
  title: string;
  departmentId: string;
  description?: string;
  requiredSkills?: RequiredSkill[];
  minExperience?: number;
  maxCapacity?: number;
  status?: PositionStatus;
  reportsTo?: string;
  metadata?: Record<string, unknown>;
}

// =============================================================================
// ASSIGNMENT TYPES
// =============================================================================

export type AssignmentStatus = 'active' | 'on_leave' | 'suspended' | 'terminated';
export type AssignmentType = 'permanent' | 'temporary' | 'acting';

export interface ARAssignment {
  id: string;
  organizationId: string;
  agentId: string;
  positionId: string;
  humanSupervisor?: string | null;
  assignmentType: AssignmentType;
  startDate?: string | null;
  endDate?: string | null;
  status: AssignmentStatus;
  performanceScore?: number | null;
  workload: number;
  metadata?: Record<string, unknown>;
  createdAt: string;
  updatedAt: string;
}

export interface CreateAssignmentInput {
  agentId: string;
  positionId: string;
  humanSupervisor?: string;
  assignmentType?: AssignmentType;
  startDate?: string;
  endDate?: string;
  status?: AssignmentStatus;
  performanceScore?: number;
  workload?: number;
  metadata?: Record<string, unknown>;
}

// =============================================================================
// APPROVAL TYPES
// =============================================================================

export type ApprovalLevel = 1 | 2 | 3 | 4 | 5;
export type ApprovalStatus = 'pending' | 'approved' | 'rejected' | 'expired' | 'escalated';
export type RequestType = 'task' | 'budget' | 'assignment' | 'schedule' | 'leave' | 'policy';
export type RequesterType = 'agent' | 'human';

export interface ApproverChainEntry {
  level: number;
  approverId: string;
  approverType: RequesterType;
  roleTitle?: string;
}

export interface ApprovalResponse {
  level: number;
  approverId: string;
  decision: 'approved' | 'rejected';
  note?: string;
  timestamp: string;
}

export interface ARApprovalRequest {
  id: string;
  organizationId: string;
  requestType: RequestType;
  level: ApprovalLevel;
  title: string;
  description: string;
  context: Record<string, unknown>;
  impactScope?: string;
  estimatedValue?: number;
  requesterType: RequesterType;
  requesterId: string;
  approverChain: ApproverChainEntry[];
  currentLevel: number;
  status: ApprovalStatus;
  responses: ApprovalResponse[];
  expiresAt: string;
  escalationAt?: string;
  slackChannelId?: string;
  slackMessageTs?: string;
  createdAt: string;
  updatedAt: string;
}

// =============================================================================
// COORDINATION TYPES
// =============================================================================

export type IssueSeverity = 'critical' | 'high' | 'medium' | 'low';
export type IssueCategory = 'workload' | 'performance' | 'deadline' | 'resource' | 'skill_gap';

export interface Issue {
  id: string;
  category: IssueCategory;
  severity: IssueSeverity;
  title: string;
  description: string;
  affectedAgents: string[];
  suggestedActions: string[];
  detectedAt: string;
}

export interface IssueSummary {
  total: number;
  bySeverity: {
    critical: number;
    high: number;
    medium: number;
    low: number;
  };
  byCategory: Record<string, number>;
  topIssues: Issue[];
}

export interface WorkloadSnapshot {
  agentId: string;
  agentName: string;
  currentWorkload: number;
  targetWorkload: number;
  taskCount: number;
  status: 'overloaded' | 'balanced' | 'underutilized';
}

export interface WorkloadAnalysis {
  stats: {
    avgWorkload: number;
    stdDeviation: number;
    totalAgents: number;
    overloaded: number;
    underutilized: number;
    balanced: number;
  };
  snapshots: WorkloadSnapshot[];
}

export interface RebalanceChange {
  id: number;
  fromAgentId: string;
  toAgentId: string;
  taskIds: string[];
  estimatedImpact: {
    fromWorkload: { before: number; after: number };
    toWorkload: { before: number; after: number };
  };
}

export interface RebalanceProposal {
  id: string;
  organizationId: string;
  trigger: string;
  proposedChanges: RebalanceChange[];
  estimatedImprovement: number;
  status: 'pending' | 'applied' | 'rejected';
  createdAt: string;
}

// =============================================================================
// ANALYTICS TYPES
// =============================================================================

export type HealthStatus = 'healthy' | 'warning' | 'critical';

export interface AgentHealthCheck {
  agentId: string;
  agentName: string;
  status: HealthStatus;
  workload: number;
  issues: string[];
  lastActive: string;
}

export interface RecommendationType =
  | 'template'
  | 'team_composition'
  | 'position'
  | 'skill_development'
  | 'resource_allocation'
  | 'structure_optimization';

export interface Recommendation {
  id: string;
  type: RecommendationType;
  priority: 'critical' | 'high' | 'medium' | 'low';
  title: string;
  description: string;
  actions: Array<{
    label: string;
    action: string;
    params?: Record<string, unknown>;
  }>;
  estimatedImpact?: string;
  createdAt: string;
}

// =============================================================================
// DASHBOARD TYPES
// =============================================================================

export interface ARDashboardStats {
  totalDepartments: number;
  totalPositions: number;
  activeAssignments: number;
  pendingApprovals: number;
}
```

### 4.2 API 클라이언트 훅 예시

```typescript
// frontend/src/hooks/ar/useARDepartments.ts

import { useState, useCallback } from 'react';
import { request, ApiError } from '../../api/client';
import { ARDepartment, CreateDepartmentInput, UpdateDepartmentInput } from '../../types/ar';

interface DepartmentsResponse {
  departments: ARDepartment[];
  total: number;
}

interface DepartmentFilters {
  status?: string;
  parentId?: string;
  search?: string;
}

export function useARDepartments() {
  const [departments, setDepartments] = useState<ARDepartment[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchDepartments = useCallback(async (filters?: DepartmentFilters) => {
    setIsLoading(true);
    setError(null);

    try {
      const data = await request<DepartmentsResponse>({
        url: '/api/ar/departments',
        method: 'GET',
        params: filters,
      });
      setDepartments(data.departments);
    } catch (err) {
      const message = err instanceof ApiError ? err.message : 'Failed to fetch departments';
      setError(message);
    } finally {
      setIsLoading(false);
    }
  }, []);

  const createDepartment = useCallback(async (input: CreateDepartmentInput) => {
    const data = await request<{ department: ARDepartment }>({
      url: '/api/ar/departments',
      method: 'POST',
      data: input,
    });
    return data.department;
  }, []);

  const updateDepartment = useCallback(async (id: string, input: UpdateDepartmentInput) => {
    const data = await request<{ department: ARDepartment }>({
      url: `/api/ar/departments/${id}`,
      method: 'PATCH',
      data: input,
    });
    return data.department;
  }, []);

  const deleteDepartment = useCallback(async (id: string) => {
    await request({
      url: `/api/ar/departments/${id}`,
      method: 'DELETE',
    });
  }, []);

  return {
    departments,
    isLoading,
    error,
    fetchDepartments,
    createDepartment,
    updateDepartment,
    deleteDepartment,
  };
}
```

---

## 5. Slack 명령어 상세 설계

### 5.1 명령어 목록

| 명령어 | 한글 별칭 | 설명 | 예시 |
|--------|----------|------|------|
| `/ar status` | `/ar 상태` | AR 시스템 전체 현황 | `/ar status` |
| `/ar workload` | `/ar 워크로드` | 워크로드 분포 조회 | `/ar workload` |
| `/ar issues` | `/ar 이슈` | 현재 이슈 목록 | `/ar issues` |
| `/ar approve <id>` | `/ar 승인 <id>` | 결재 승인 | `/ar approve abc123` |
| `/ar reject <id>` | `/ar 반려 <id>` | 결재 반려 | `/ar reject abc123 -r "예산 초과"` |
| `/ar help` | `/ar 도움말` | 명령어 도움말 | `/ar help` |

### 5.2 명령어 응답 포맷

#### `/ar status` 응답
```
📊 *AR System Status*

*Overview:*
• 부서: 5개
• 포지션: 12개
• 활성 배치: 8개
• 대기 결재: 3건

*Workload Distribution:*
🟢 정상: 5 에이전트
🟡 주의: 2 에이전트
🔴 과부하: 1 에이전트

*Issues:*
⚠️ 2건의 이슈가 감지되었습니다
└ 자세히 보기: `/ar issues`
```

#### `/ar workload` 응답
```
📈 *Workload Distribution*

| Agent          | Workload | Status    |
|----------------|----------|-----------|
| agent-alpha    |    82%   | 🟢 Normal |
| agent-beta     |    95%   | 🔴 High   |
| agent-gamma    |    58%   | 🟢 Normal |
| agent-delta    |    22%   | 🟡 Low    |

*Average:* 64%
*Recommendation:* 워크로드 리밸런싱을 권장합니다

[🔄 리밸런싱 제안 보기]
```

#### `/ar issues` 응답
```
🚨 *Active Issues*

*Critical:* 0
*High:* 1
*Medium:* 1
*Low:* 0

---

🔴 *[HIGH] 과부하 에이전트 감지*
agent-beta 워크로드 95% - 작업 분산 필요
└ 권장: 3건의 작업을 agent-delta로 이전

🟡 *[MEDIUM] 목표 정렬 미달*
agent-gamma 목표 달성률 45%
└ 권장: 코칭 세션 예약
```

### 5.3 Interactive Components

#### 결재 요청 버튼
```
┌────────────────────────────────────────────┐
│ 🔔 *New Approval Request*                  │
│                                            │
│ *예산 증액 요청*                           │
│ Q2 마케팅 예산 $5,000 증액 필요            │
│                                            │
│ 요청자: agent-alpha                        │
│ 레벨: 3 (PROJECT)                          │
│ 영향: team                                 │
│                                            │
│ [✅ 승인]  [❌ 반려]  [📝 상세보기]        │
└────────────────────────────────────────────┘
```

### 5.4 구현 코드 (src/api/slack-ar-commands.ts)

```typescript
import { Router, Request, Response } from "express";
import crypto from "crypto";
import { db as prisma } from "../db/client";
import { logger } from "../utils/logger";
import { arDepartmentService } from "../ar/organization/ar-department.service";
import { arAssignmentService } from "../ar/organization/ar-assignment.service";
import { arApprovalService } from "../ar/approval/ar-approval.service";
import { issueDetectorService } from "../ar/coordination/issue-detector.service";
import { workloadRebalancerService } from "../ar/coordination/workload-rebalancer.service";
import { getUserBySlackId, getOrganizationBySlackWorkspace } from "../services/slack-service";

const router = Router();

// 한글 명령어 매핑
const COMMAND_ALIASES: Record<string, string> = {
  '상태': 'status',
  '워크로드': 'workload',
  '이슈': 'issues',
  '승인': 'approve',
  '반려': 'reject',
  '도움말': 'help',
};

// Slack 서명 검증 함수 (기존 패턴 재사용)
function verifySlackSignature(
  signingSecret: string,
  signature: string,
  timestamp: string,
  body: string,
): boolean {
  const fiveMinutesAgo = Math.floor(Date.now() / 1000) - 60 * 5;
  if (parseInt(timestamp, 10) < fiveMinutesAgo) {
    return false;
  }

  const sigBasestring = `v0:${timestamp}:${body}`;
  const hmac = crypto.createHmac("sha256", signingSecret);
  hmac.update(sigBasestring);
  const computedSignature = `v0=${hmac.digest("hex")}`;

  try {
    return crypto.timingSafeEqual(
      Buffer.from(computedSignature, "utf-8"),
      Buffer.from(signature, "utf-8"),
    );
  } catch {
    return false;
  }
}

// 명령어 파서
function parseCommand(text: string): { command: string; args: string[] } {
  const parts = text.trim().split(/\s+/);
  let command = parts[0]?.toLowerCase() || 'help';

  // 한글 명령어 변환
  if (COMMAND_ALIASES[command]) {
    command = COMMAND_ALIASES[command];
  }

  return {
    command,
    args: parts.slice(1),
  };
}

router.post("/commands", async (req: Request, res: Response) => {
  try {
    const signingSecret = process.env.SLACK_SIGNING_SECRET;
    if (!signingSecret) {
      return res.status(500).json({ error: "Slack not configured" });
    }

    const slackSignature = req.headers["x-slack-signature"] as string;
    const slackTimestamp = req.headers["x-slack-request-timestamp"] as string;

    if (!slackSignature || !slackTimestamp) {
      return res.status(401).json({ error: "Missing signature" });
    }

    const rawBody = typeof (req as any).rawBody === "string"
      ? (req as any).rawBody
      : new URLSearchParams(req.body).toString();

    if (!verifySlackSignature(signingSecret, slackSignature, slackTimestamp, rawBody)) {
      return res.status(401).json({ error: "Invalid signature" });
    }

    const { team_id: teamId, user_id: slackUserId, text } = req.body;

    // 조직 조회
    const organization = await getOrganizationBySlackWorkspace(teamId);
    if (!organization) {
      return res.status(200).json({
        response_type: "ephemeral",
        text: "Slack 워크스페이스가 Nubabel에 연결되지 않았습니다.",
      });
    }

    const { command, args } = parseCommand(text || '');

    switch (command) {
      case 'status':
        return handleStatus(res, organization.id);
      case 'workload':
        return handleWorkload(res, organization.id);
      case 'issues':
        return handleIssues(res, organization.id);
      case 'approve':
        return handleApprove(res, organization.id, slackUserId, args);
      case 'reject':
        return handleReject(res, organization.id, slackUserId, args);
      case 'help':
      default:
        return handleHelp(res);
    }
  } catch (error) {
    logger.error("AR Slack command error", { error });
    return res.status(200).json({
      response_type: "ephemeral",
      text: "명령어 처리 중 오류가 발생했습니다.",
    });
  }
});

async function handleStatus(res: Response, orgId: string) {
  const [departments, positions, assignments, issues] = await Promise.all([
    arDepartmentService.findAll(orgId, { status: 'active' }),
    prisma.aRPosition.count({ where: { organizationId: orgId, status: 'active' } }),
    arAssignmentService.findAll(orgId, { status: 'active' }),
    issueDetectorService.detectIssues(orgId),
  ]);

  const workload = await workloadRebalancerService.analyzeWorkloadDistribution(orgId);

  return res.status(200).json({
    response_type: "in_channel",
    blocks: [
      {
        type: "header",
        text: { type: "plain_text", text: "📊 AR System Status", emoji: true }
      },
      {
        type: "section",
        fields: [
          { type: "mrkdwn", text: `*부서:*\n${departments.length}개` },
          { type: "mrkdwn", text: `*포지션:*\n${positions}개` },
          { type: "mrkdwn", text: `*활성 배치:*\n${assignments.length}개` },
          { type: "mrkdwn", text: `*이슈:*\n${issues.issues.length}건` },
        ]
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: `*Workload:*\n🟢 정상: ${workload.balanced.length} | 🟡 저활용: ${workload.underutilized.length} | 🔴 과부하: ${workload.overloaded.length}`
        }
      },
      {
        type: "context",
        elements: [
          { type: "mrkdwn", text: `평균 워크로드: ${Math.round(workload.avgWorkload * 100)}%` }
        ]
      }
    ]
  });
}

async function handleWorkload(res: Response, orgId: string) {
  const analysis = await workloadRebalancerService.analyzeWorkloadDistribution(orgId);

  const agentRows = analysis.snapshots.slice(0, 10).map(s => {
    const bar = '█'.repeat(Math.floor(s.currentWorkload * 10)) + '░'.repeat(10 - Math.floor(s.currentWorkload * 10));
    const status = s.status === 'overloaded' ? '🔴' : s.status === 'underutilized' ? '🟡' : '🟢';
    return `${s.agentName.padEnd(15)} ${bar} ${Math.round(s.currentWorkload * 100)}% ${status}`;
  }).join('\n');

  return res.status(200).json({
    response_type: "in_channel",
    blocks: [
      {
        type: "header",
        text: { type: "plain_text", text: "📈 Workload Distribution", emoji: true }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "```\n" + agentRows + "\n```"
        }
      },
      {
        type: "section",
        fields: [
          { type: "mrkdwn", text: `*평균:*\n${Math.round(analysis.avgWorkload * 100)}%` },
          { type: "mrkdwn", text: `*표준편차:*\n${Math.round(analysis.stdDeviation * 100)}%` },
        ]
      },
      {
        type: "actions",
        elements: [
          {
            type: "button",
            text: { type: "plain_text", text: "🔄 리밸런싱 제안", emoji: true },
            action_id: "ar_rebalance_propose",
            value: orgId
          }
        ]
      }
    ]
  });
}

async function handleIssues(res: Response, orgId: string) {
  const result = await issueDetectorService.detectIssues(orgId);

  if (result.issues.length === 0) {
    return res.status(200).json({
      response_type: "ephemeral",
      text: "✅ 현재 감지된 이슈가 없습니다."
    });
  }

  const issueBlocks = result.issues.slice(0, 5).map(issue => {
    const emoji = issue.severity === 'critical' ? '🔴' :
                  issue.severity === 'high' ? '🟠' :
                  issue.severity === 'medium' ? '🟡' : '🟢';
    return {
      type: "section",
      text: {
        type: "mrkdwn",
        text: `${emoji} *[${issue.severity.toUpperCase()}] ${issue.title}*\n${issue.description}\n_권장: ${issue.suggestedActions[0] || 'N/A'}_`
      }
    };
  });

  return res.status(200).json({
    response_type: "in_channel",
    blocks: [
      {
        type: "header",
        text: { type: "plain_text", text: "🚨 Active Issues", emoji: true }
      },
      {
        type: "section",
        fields: [
          { type: "mrkdwn", text: `*Critical:* ${result.issues.filter(i => i.severity === 'critical').length}` },
          { type: "mrkdwn", text: `*High:* ${result.issues.filter(i => i.severity === 'high').length}` },
          { type: "mrkdwn", text: `*Medium:* ${result.issues.filter(i => i.severity === 'medium').length}` },
          { type: "mrkdwn", text: `*Low:* ${result.issues.filter(i => i.severity === 'low').length}` },
        ]
      },
      { type: "divider" },
      ...issueBlocks
    ]
  });
}

async function handleApprove(res: Response, orgId: string, slackUserId: string, args: string[]) {
  const requestId = args[0];
  if (!requestId) {
    return res.status(200).json({
      response_type: "ephemeral",
      text: "사용법: `/ar approve <request_id>`"
    });
  }

  try {
    const request = await arApprovalService.findById(requestId);
    if (!request || request.organizationId !== orgId) {
      return res.status(200).json({
        response_type: "ephemeral",
        text: "결재 요청을 찾을 수 없습니다."
      });
    }

    // 승인자 ID 조회 (Slack ID -> User ID 변환 필요)
    const user = await getUserBySlackId(slackUserId, null as any); // WebClient 필요
    if (!user) {
      return res.status(200).json({
        response_type: "ephemeral",
        text: "사용자 정보를 찾을 수 없습니다."
      });
    }

    await arApprovalService.respond(requestId, 'approved', user.id);

    return res.status(200).json({
      response_type: "in_channel",
      text: `✅ 결재가 승인되었습니다: ${request.title}`
    });
  } catch (error) {
    const message = error instanceof Error ? error.message : '알 수 없는 오류';
    return res.status(200).json({
      response_type: "ephemeral",
      text: `승인 처리 실패: ${message}`
    });
  }
}

async function handleReject(res: Response, orgId: string, slackUserId: string, args: string[]) {
  const requestId = args[0];
  const reasonIndex = args.indexOf('-r');
  const reason = reasonIndex >= 0 ? args.slice(reasonIndex + 1).join(' ') : undefined;

  if (!requestId) {
    return res.status(200).json({
      response_type: "ephemeral",
      text: "사용법: `/ar reject <request_id> [-r 사유]`"
    });
  }

  try {
    const request = await arApprovalService.findById(requestId);
    if (!request || request.organizationId !== orgId) {
      return res.status(200).json({
        response_type: "ephemeral",
        text: "결재 요청을 찾을 수 없습니다."
      });
    }

    const user = await getUserBySlackId(slackUserId, null as any);
    if (!user) {
      return res.status(200).json({
        response_type: "ephemeral",
        text: "사용자 정보를 찾을 수 없습니다."
      });
    }

    await arApprovalService.respond(requestId, 'rejected', user.id, reason);

    return res.status(200).json({
      response_type: "in_channel",
      text: `❌ 결재가 반려되었습니다: ${request.title}${reason ? `\n사유: ${reason}` : ''}`
    });
  } catch (error) {
    const message = error instanceof Error ? error.message : '알 수 없는 오류';
    return res.status(200).json({
      response_type: "ephemeral",
      text: `반려 처리 실패: ${message}`
    });
  }
}

function handleHelp(res: Response) {
  return res.status(200).json({
    response_type: "ephemeral",
    blocks: [
      {
        type: "header",
        text: { type: "plain_text", text: "📖 AR Commands Help", emoji: true }
      },
      {
        type: "section",
        text: {
          type: "mrkdwn",
          text: "*Available Commands:*\n\n" +
            "• `/ar status` (상태) - AR 시스템 전체 현황\n" +
            "• `/ar workload` (워크로드) - 워크로드 분포 조회\n" +
            "• `/ar issues` (이슈) - 현재 이슈 목록\n" +
            "• `/ar approve <id>` (승인) - 결재 승인\n" +
            "• `/ar reject <id> [-r 사유]` (반려) - 결재 반려\n" +
            "• `/ar help` (도움말) - 이 도움말 표시"
        }
      },
      {
        type: "context",
        elements: [
          { type: "mrkdwn", text: "_한글/영어 명령어 모두 지원됩니다_" }
        ]
      }
    ]
  });
}

export default router;
```

---

## 6. 구현 순서 (의존성 기반)

### Phase 1: 기반 작업 (Day 1)

```
1.1 타입 정의
    └── frontend/src/types/ar.ts

1.2 API 훅 구현
    ├── useARDepartments.ts
    ├── useARPositions.ts
    ├── useARAssignments.ts
    ├── useARApprovals.ts
    ├── useARWorkload.ts
    ├── useARAnalytics.ts
    └── useARIssues.ts

1.3 공통 컴포넌트 구현
    ├── ARStatCard.tsx
    ├── AgentHealthIndicator.tsx
    └── IssueAlert.tsx
```

### Phase 2: 핵심 페이지 (Day 2)

```
2.1 AR Dashboard
    └── ARDashboardPage.tsx
        ├── 통계 카드 4개
        ├── 워크로드 분포 차트
        ├── 최근 이슈 목록
        └── 추천 사항

2.2 부서 관리
    └── ARDepartmentsPage.tsx
        ├── DepartmentTree.tsx
        ├── DepartmentCard.tsx
        ├── CreateDepartmentModal.tsx
        └── EditDepartmentModal.tsx
```

### Phase 3: CRUD 페이지 (Day 3)

```
3.1 포지션 관리
    └── ARPositionsPage.tsx
        ├── PositionCard.tsx
        ├── CreatePositionModal.tsx
        └── EditPositionModal.tsx

3.2 배치 관리
    └── ARAssignmentsPage.tsx
        ├── AssignmentCard.tsx
        ├── CreateAssignmentModal.tsx
        ├── EditAssignmentModal.tsx
        └── ReassignModal.tsx
```

### Phase 4: 고급 기능 (Day 4)

```
4.1 결재 관리
    └── ARApprovalsPage.tsx
        └── ApprovalChainView.tsx

4.2 분석 페이지
    └── ARAnalyticsPage.tsx
        └── 차트 컴포넌트들

4.3 워크로드 뷰어
    └── ARWorkloadPage.tsx
        ├── WorkloadChart.tsx
        └── WorkloadDistributionBar.tsx
```

### Phase 5: Slack 통합 (Day 5)

```
5.1 Slack 명령어 핸들러
    └── src/api/slack-ar-commands.ts

5.2 라우트 등록
    └── src/index.ts 수정

5.3 네비게이션 및 라우팅
    ├── frontend/src/App.tsx 수정
    └── frontend/src/components/layout/Sidebar.tsx 수정
```

---

## 7. 테스트 계획

### 7.1 단위 테스트

| 대상 | 테스트 파일 | 우선순위 |
|------|------------|----------|
| API 훅 | `useARDepartments.test.ts` | HIGH |
| 타입 검증 | `ar.types.test.ts` | MEDIUM |
| Slack 명령어 파서 | `slack-ar-commands.test.ts` | HIGH |

### 7.2 통합 테스트

| 시나리오 | 테스트 방법 |
|----------|------------|
| 부서 CRUD | API 호출 → DB 검증 → UI 반영 |
| 결재 플로우 | 요청 생성 → 승인/반려 → 상태 변경 |
| 워크로드 리밸런싱 | 제안 생성 → 적용 → 결과 검증 |

### 7.3 E2E 테스트 (Playwright)

```typescript
// tests/ar-dashboard.spec.ts
import { test, expect } from '@playwright/test';

test.describe('AR Dashboard', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/login');
    // 로그인 처리
    await page.goto('/ar');
  });

  test('displays stats cards', async ({ page }) => {
    await expect(page.getByText('총 부서')).toBeVisible();
    await expect(page.getByText('총 포지션')).toBeVisible();
    await expect(page.getByText('활성 배치')).toBeVisible();
    await expect(page.getByText('대기 결재')).toBeVisible();
  });

  test('navigates to departments page', async ({ page }) => {
    await page.click('text=Departments');
    await expect(page).toHaveURL('/ar/departments');
  });
});
```

### 7.4 Slack 명령어 테스트

```bash
# 로컬 테스트 (ngrok 사용)
ngrok http 3000

# Slack App 설정에서 Request URL 업데이트
# https://<ngrok-url>/api/slack/ar/commands

# 테스트 명령어
/ar status
/ar 상태
/ar workload
/ar 워크로드
/ar issues
/ar 이슈
/ar help
/ar 도움말
```

---

## 8. 예상 리스크 및 대응방안

### 8.1 기술적 리스크

| 리스크 | 영향도 | 가능성 | 대응방안 |
|--------|--------|--------|----------|
| 기존 AR API 버그 | HIGH | LOW | API 호출 전 백엔드 테스트 선행 |
| Slack 서명 검증 실패 | MEDIUM | MEDIUM | 기존 검증 로직 재사용, 로깅 강화 |
| 대용량 데이터 렌더링 | MEDIUM | MEDIUM | 페이지네이션, 가상 스크롤 적용 |
| 타입 불일치 | LOW | HIGH | 백엔드 타입과 프론트엔드 타입 동기화 |

### 8.2 UX 리스크

| 리스크 | 영향도 | 가능성 | 대응방안 |
|--------|--------|--------|----------|
| 복잡한 결재 체인 이해 | MEDIUM | HIGH | 시각화 컴포넌트로 단계별 표시 |
| 워크로드 수치 해석 | LOW | MEDIUM | 툴팁, 색상 코딩으로 직관성 향상 |
| 한글/영어 혼용 | LOW | LOW | 일관된 UI 언어 정책 적용 |

### 8.3 일정 리스크

| 리스크 | 영향도 | 가능성 | 대응방안 |
|--------|--------|--------|----------|
| Phase 1 지연 | HIGH | LOW | 타입/훅 작업은 병렬화 가능 |
| Slack 통합 이슈 | MEDIUM | MEDIUM | Day 5에 버퍼 포함, 필요시 일부 명령어 후순위 |
| 코드 리뷰 지연 | LOW | MEDIUM | PR 사이즈 제한 (300줄 이하) |

---

## 9. 완료 기준 (Definition of Done)

### 9.1 기능 완료 기준

- [ ] 7개 페이지 모두 구현 및 라우팅 연결
- [ ] 모든 AR API 엔드포인트 프론트엔드 연동
- [ ] 6개 Slack 명령어 (한글/영어) 동작 확인
- [ ] 에러 처리 및 로딩 상태 표시

### 9.2 품질 기준

- [ ] TypeScript 컴파일 에러 0건
- [ ] ESLint 경고 0건 (기존 규칙 준수)
- [ ] 주요 플로우 E2E 테스트 통과
- [ ] Lighthouse 접근성 점수 90점 이상

### 9.3 문서화 기준

- [ ] 컴포넌트 JSDoc 주석
- [ ] API 훅 사용 예시
- [ ] Slack 명령어 사용 가이드

---

## Appendix A: Sidebar 메뉴 추가 위치

```typescript
// frontend/src/components/layout/Sidebar.tsx

const organizationNavItems: NavItem[] = [
  { name: "Members", path: "/settings/members", icon: "👥" },
  { name: "Approvals", path: "/approvals", icon: "✅" },
  { name: "Changes", path: "/org-changes", icon: "📊" },
];

// 추가할 AR 섹션
const arNavItems: NavItem[] = [
  { name: "AR Dashboard", path: "/ar", icon: "🏢" },
  { name: "Departments", path: "/ar/departments", icon: "🏛️" },
  { name: "Positions", path: "/ar/positions", icon: "💼" },
  { name: "Assignments", path: "/ar/assignments", icon: "👤" },
  { name: "AR Approvals", path: "/ar/approvals", icon: "📋" },
  { name: "Analytics", path: "/ar/analytics", icon: "📊" },
  { name: "Workload", path: "/ar/workload", icon: "⚖️" },
];
```

---

## Appendix B: App.tsx 라우트 추가

```typescript
// frontend/src/App.tsx

// 신규 import
import ARDashboardPage from "./pages/ar/ARDashboardPage";
import ARDepartmentsPage from "./pages/ar/ARDepartmentsPage";
import ARPositionsPage from "./pages/ar/ARPositionsPage";
import ARAssignmentsPage from "./pages/ar/ARAssignmentsPage";
import ARApprovalsPage from "./pages/ar/ARApprovalsPage";
import ARAnalyticsPage from "./pages/ar/ARAnalyticsPage";
import ARWorkloadPage from "./pages/ar/ARWorkloadPage";

// 라우트 추가 (기존 패턴 따름)
<Route
  path="/ar"
  element={
    <ProtectedRoute>
      <DashboardLayout>
        <ARDashboardPage />
      </DashboardLayout>
    </ProtectedRoute>
  }
/>
<Route
  path="/ar/departments"
  element={
    <ProtectedRoute>
      <DashboardLayout>
        <ARDepartmentsPage />
      </DashboardLayout>
    </ProtectedRoute>
  }
/>
// ... 나머지 6개 라우트
```

---

## Appendix C: Backend 라우트 등록

```typescript
// src/index.ts

import slackARCommandsRoutes from "./api/slack-ar-commands";

// 기존 라우트들 아래에 추가
app.use("/api/slack/ar", slackARCommandsRoutes);
```

---

*기획서 작성 완료: 2026-01-30*
*Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>*
