# Test Environment Configuration
# Copy this file to .env.test and fill in the values

# Database (use separate test database!)
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/nubabel_test"

# Redis (use separate test instance if possible)
TEST_REDIS_URL="redis://localhost:6379/1"

# Encryption key for test accounts (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY="your-64-char-hex-encryption-key-here"

# Optional: Slack (for integration tests)
SLACK_BOT_TOKEN=""
SLACK_APP_TOKEN=""
SLACK_SIGNING_SECRET=""
