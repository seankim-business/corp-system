## 2026-01-26

- Chose MCP span naming `mcp.<provider>.<tool>` with camelCase→snake_case conversion for tool names.
- Used custom in-test context manager to validate parent-child span relationships without extra dependencies.
